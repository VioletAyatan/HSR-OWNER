# 项目协作规则

## 开始工作

- 先核对当前源码、`git status` 和相关差异；保留已有修改及暂存状态，避免重复全仓库分析。
- 只推进用户当前请求。不要因为文档中存在历史任务就启动游戏、部署产物或修改无关业务。
- 架构资料按需阅读，不作为每次对话的必读内容。

## 修改与验证

- 从整体代码考虑修复，检查调用者、共享缓存、异常传播、失败重试和输出兼容性；不能靠跳过错误或破坏其他模块来完成当前流程。
- 索引范围取自当次元数据，不写死当前游戏版本的数量或人为上限。取值前检查逻辑边界、地址溢出、空指针和内存可读性；可读不代表地址属于目标数组或对象。
- 排查和修复要有日志：记录阶段、关键上下文、失败原因和完成摘要。高频循环采用限频心跳/汇总，诊断缓存必须有界，避免逐条强制写盘及无界内存增长。
- 根据改动运行必要的测试与构建，区分静态分析、离线回放和游戏内验证。前端 Finished 只说明流程完成，还需检查相关产物能否解析和使用；未验证范围要明确说明。
- 格式化 Rust 时只处理修改文件，使用 `rustfmt --edition 2024 --config skip_children=true <文件>`，避免递归格式化破坏 IL2CPP 宏代码。

## 当前状态与入口

- Resources 修复已通过游戏内流程验收并提交；Proto/WriteTo 的卡住、崩溃修复也已通过一次完整流程验证，不再作为待排查问题。
- 当前未解决：导出的 GateServer 存在字段重名，导致 protobuf 解析失败。继续此项时读 [Proto 当前问题](docs/Proto排查工作流与交接.md)。
- 本机游戏目录：`D:/StarRail_Beta`；日志：`hsr-owner.log`；导出目录：`DUMP`。

## 文档维护

- AGENTS.md 只保留长期规则、当前状态和必要入口；任务文档只保留未解决问题的证据、定位线索、下一步及验证边界。
- 问题修复且验证完成后，删除对应排查文档或已完成章节，并清理引用。不保留逐轮过程、旧失败日志、过期待办或重复验收记录；历史代码改动查 Git。
